services:
  - type: web
    name: ai-chatbot-frontend
    runtime: static
    buildCommand: npm install && npm run build
    publishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://ai-chatbot-backend-d3gn.onrender.com
